{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @module-federation-ts/* --parallel start",
    "build": "lerna run --scope @module-federation-ts/* build",
    "serve": "lerna run --scope @module-federation-ts/* --parallel serve",
    "clean": "lerna run --scope @module-federation-ts/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
